-add bot to scrape r/wsb for additional stocks

-add way to pick target week from ui
-search function from ui (find if in array or compute new card if not)
-timestamp data


-store data in db/ make API calls automatically per x time
-better rate limiting (low priority)
-dockerize/ deploy